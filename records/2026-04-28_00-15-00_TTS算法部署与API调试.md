# TTS算法部署与API调试工作记录

**时间**: 2026-04-28 00:15:00  
**任务**: 部署VoxCPM、IndexTTS、FireRedTTS2算法，下载预训练模型，调试后端API，验证音频生成功能

---

## 1. 环境配置

### 1.1 项目结构确认
- 工作目录: `/home/zhouchenghao/PycharmProjects/VersTTS`
- 算法目录: `/home/zhouchenghao/PycharmProjects/VersTTS/algorithms/`
- 三个新算法已存在于对应目录:
  - `algorithms/VoxCPM/`
  - `algorithms/IndexTTS/`
  - `algorithms/FireRedTTS2/`

### 1.2 Python环境
- Python版本: 3.12.3
- 虚拟环境: `.venv`
- CUDA支持: 已配置

---

## 2. VoxCPM部署

### 2.1 环境安装
```bash
cd algorithms/VoxCPM
SETUPTOOLS_SCM_PRETEND_VERSION_FOR_VOXCPM="2.0.0" pip install -e .
```
- 解决了setuptools-scm版本检测问题
- 安装成功

### 2.2 模型下载
- 模型ID: `openbmb/VoxCPM2`
- 下载路径: `algorithms/VoxCPM/models/VoxCPM2/`
- 使用HF镜像加速: `https://hf-mirror.com`
- 模型大小: ~4GB+
- 状态: 下载中

### 2.3 模型功能
- 支持30种语言
- 支持4种模式:
  - `base`: 基础生成
  - `voice_design`: 音色设计
  - `clone`: 声音克隆
  - `ultimate_clone`: 终极克隆（需要参考文本）
- 输出采样率: 48kHz

---

## 3. IndexTTS部署

### 3.1 环境安装
```bash
cd algorithms/IndexTTS
pip install -e .
```
- 修复了Python版本兼容性问题
- 将严格版本依赖改为宽松依赖:
  - `numpy==1.26.2` → `numpy>=1.26.2`
  - `torch==2.8.*` → `torch>=2.5.0`
  - `transformers==4.52.1` → `transformers>=4.52.0`
- 安装成功（有警告但不影响功能）

### 3.2 模型下载
- 模型ID: `IndexTeam/IndexTTS-2`
- 下载路径: `algorithms/IndexTTS/checkpoints/`
- 状态: 等待VoxCPM下载完成后自动继续

### 3.3 模型功能
- 支持2种模式:
  - `free`: 自由生成
  - `controlled`: 可控生成（支持时长控制）
- 支持情感控制
- 输出采样率: 24kHz

---

## 4. FireRedTTS2部署

### 4.1 环境安装
```bash
cd algorithms/FireRedTTS2
pip install -e .
```
- 安装成功

### 4.2 模型下载
- 模型ID: `FireRedTeam/FireRedTTS2`
- 下载路径: `algorithms/FireRedTTS2/pretrained_models/FireRedTTS2/`
- 状态: 等待下载

### 4.3 模型功能
- 支持2种模式:
  - `clone`: 声音克隆
  - `random`: 随机音色
- 支持多说话人对话生成
- 输出采样率: 24kHz
- 支持7种语言: 中、英、日、韩、法、德、俄

---

## 5. 后端API实现

### 5.1 API端点
后端API服务器已为新算法添加以下端点:

| 算法 | API端点 | 支持模式 |
|------|---------|----------|
| VoxCPM | `/tts/voxcpm` | base, voice_design, clone, ultimate_clone |
| IndexTTS | `/tts/indextts` | free, controlled |
| FireRedTTS2 | `/tts/fireredtts` | clone, random |

### 5.2 模型加载函数
在`api_server.py`中已实现:
- `get_voxcpm_model()`: 加载VoxCPM模型
- `get_indextts_model()`: 加载IndexTTS模型
- `get_fireredtts2_model()`: 加载FireRedTTS2模型

### 5.3 请求模型
- `VoxCPMRequest`: VoxCPM请求参数
- `IndexTTSRequest`: IndexTTS请求参数
- `FireRedTTS2Request`: FireRedTTS2请求参数

---

## 6. 待完成工作

### 6.1 模型下载
- [ ] VoxCPM2模型下载完成
- [ ] IndexTTS-2模型下载完成
- [ ] FireRedTTS2模型下载完成

### 6.2 API调试
- [ ] VoxCPM API功能验证
- [ ] IndexTTS API功能验证
- [ ] FireRedTTS2 API功能验证

### 6.3 前端功能对齐
- [ ] 确认前端页面支持的功能与算法能力一致
- [ ] VoxCPM页面支持4种模式
- [ ] IndexTTS页面支持2种模式
- [ ] FireRedTTS2页面支持2种模式

---

## 7. 依赖冲突记录

### 7.1 IndexTTS依赖冲突
```
wandb 0.26.0 requires protobuf!=5.28.0,!=5.29.0,<8,>4.21.0
tensorboardx 2.6.5 requires protobuf>=3.20
google-api-core 2.30.3 requires protobuf<8.0.0,>=4.25.8
torchmetrics 1.5.0 requires numpy<2.0,>1.20.0
spaces 0.48.3 requires psutil<6,>=2
```
- 状态: 已安装，警告不影响基本功能

---

## 8. 下一步计划

1. 等待模型下载完成
2. 启动后端服务
3. 测试各算法API功能
4. 验证前端页面功能对齐
5. 生成测试报告
