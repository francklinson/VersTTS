# Qwen3-TTS 移除 0.6B 模型入口

**时间戳**: 2026-05-06 22:45:00  
**任务**: 移除 Qwen3-TTS 的 0.6B 模型入口  
**状态**: 已完成

---

## 修改内容

### 1. 前端页面 (frontend/pages/qwen3tts.html)

#### 修改信息框
- **移除**: 0.6B 和 1.7B 模型的对比说明
- **改为**: 3 种生成功能的介绍（声音克隆、预设音色、音色设计）

#### 移除模型大小选择器
- **移除**: 0.6B 单选按钮选项
- **改为**: 仅保留 1.7B 模型选项，标签改为"1.7B 模型（推荐）"
- **标签修改**: "模型大小" → "模型配置"

#### 更新模型能力配置
- **移除**: `modelCapabilities['0.6B']` 配置
- **保留**: 仅 `modelCapabilities['1.7B']` 配置

### 2. 后端引擎 (backend/engines/qwen3tts_engine.py)

#### 更新文档注释
- 说明 model_size 参数仅支持 "1.7B"（0.6B 已弃用）

#### 更新 size_map
- **移除**: "0.6B": "0___6B" 映射
- **保留**: 仅 "1.7B": "1___7B" 映射

#### 添加向后兼容处理
```python
# 如果传入0.6B，自动映射到1.7B（向后兼容）
if model_size == "0.6B":
    system_logger.warning("0.6B模型已弃用，自动使用1.7B模型")
    model_size = "1.7B"
```

### 3. 后端路由 (backend/routers/tts/qwen3tts.py)

#### 添加弃用注释
- 在 model_size 参数处添加注释说明 0.6B 已弃用

#### 更新状态API
- model_size 显示为 "1.7B (仅支持)"
- model_type 显示为 "Base/CustomVoice/VoiceDesign"

---

## 向后兼容性

- 后端仍接受 0.6B 参数，但会自动映射到 1.7B 模型
- 前端不再提供 0.6B 选项，用户只能选择 1.7B

---

## 修改文件列表

1. `frontend/pages/qwen3tts.html`
2. `backend/engines/qwen3tts_engine.py`
3. `backend/routers/tts/qwen3tts.py`

---

**完成时间**: 2026-05-06 22:45:00
