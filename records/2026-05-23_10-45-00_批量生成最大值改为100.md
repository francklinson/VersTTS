# 批量生成音频最大值修改为100

**时间**: 2026-05-23 10:45:00

**任务**: 将批量生成音频的最大值从20修改为100

---

## 修改内容

### 1. 后端API修改

**文件**: `backend/routers/batch.py`

- 修改参数描述：
  ```python
  # 修改前
  count: int = Form(10, description="生成数量(2-20)"),
  
  # 修改后
  count: int = Form(10, description="生成数量(2-100)"),
  ```

- 修改验证逻辑：
  ```python
  # 修改前
  if count < 2 or count > 20:
      raise HTTPException(status_code=400, detail="生成数量必须在2-20之间")
  
  # 修改后
  if count < 2 or count > 100:
      raise HTTPException(status_code=400, detail="生成数量必须在2-100之间")
  ```

### 2. 前端页面修改

**文件**: `frontend/pages/cosyvoice.html`

- 修改输入框max属性：
  ```html
  <!-- 修改前 -->
  <input type="number" id="batchCount" class="batch-count-input" value="10" min="2" max="20" onblur="validateBatchCount()">
  
  <!-- 修改后 -->
  <input type="number" id="batchCount" class="batch-count-input" value="10" min="2" max="100" onblur="validateBatchCount()">
  ```

- 修改提示文本：
  ```html
  <!-- 修改前 -->
  <p style="font-size: 0.85rem; color: #666; margin-top: 8px;">一次生成多个音频变体，可选择最佳结果（2-20个）</p>
  
  <!-- 修改后 -->
  <p style="font-size: 0.85rem; color: #666; margin-top: 8px;">一次生成多个音频变体，可选择最佳结果（2-100个）</p>
  ```

- 修改JavaScript验证逻辑：
  ```javascript
  // 修改前
  // 限制范围 2-20
  if (value < 2) {
      value = 2;
  } else if (value > 20) {
      value = 20;
  }
  
  // 修改后
  // 限制范围 2-100
  if (value < 2) {
      value = 2;
  } else if (value > 100) {
      value = 100;
  }
  ```

### 3. 其他页面状态

以下页面已经是2-100的限制，无需修改：
- `frontend/pages/voxcpm.html` ✓
- `frontend/pages/qwen3tts.html` ✓
- `frontend/pages/omnivoice.html` ✓

---

## 影响范围

- **支持的模型**: VoxCPM、Qwen3-TTS、OmniVoice、CosyVoice
- **批量生成功能**: 用户现在可以一次性生成2-100个音频变体
- **API接口**: `/tts/batch/generate`

---

## 测试建议

1. 在前端页面测试批量生成功能，输入100验证是否能正常提交
2. 测试输入超过100时是否会被自动纠正为100
3. 测试输入小于2时是否会被自动纠正为2
4. 直接调用API测试边界值（2和100）
